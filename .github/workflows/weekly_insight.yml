name: Weekly AI Insight
on:
  schedule:
    - cron: '0 20 * * 0'
  workflow_dispatch:

jobs:
  weekly-insight:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run note
        run: echo "Use this workflow later for weekly reports, exports, or snapshots."
