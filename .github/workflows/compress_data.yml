name: Monthly CSV Archive
on:
  schedule:
    - cron: '0 2 1 * *'
  workflow_dispatch:
jobs:
  compress:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Compression step"
